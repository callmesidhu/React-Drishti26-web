# Base URL of the Drishti26-web Django API.
# Leave empty in development - the Vite dev server proxies /api to http://localhost:8000.
# Set an absolute URL (e.g. https://api.example.com) only if the API is served from a different origin in production.
VITE_API_URL=
